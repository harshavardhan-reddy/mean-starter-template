var appServices = angular.module('appServices', []);

//Define your service in a separate file and bind them here
//Same way Controllers are Defined
//This way there aren't many global variables if all the code in minified into a single file;
